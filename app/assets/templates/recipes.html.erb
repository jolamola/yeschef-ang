<div class="container">
	<div ng-controller="recipesController">
		<!-- <div class="container"> -->

		<div class="row title">Recipes</div>

		<div class="row">
			<div ng-repeat="recipe in recipes" class="repeater">
				<div class="col-md-3">

					<div class="recipe-card foodimage1" ui-sref="showrecipe({id: recipe.id})">
						<!-- <img ng-src="{{ recipe.image }}"> -->
						{{ recipe.size }}
						<div class="hover-text">
							{{recipe.name}}
						</div>			
						
					</div>
					
				</div>
			</div>
		</div>
		<!-- </div> -->
	</div>
</div>